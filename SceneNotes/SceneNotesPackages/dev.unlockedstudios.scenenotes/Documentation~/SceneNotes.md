# Scene Notes

Scene Notes adds designer-facing annotations to GameObjects and displays them in editor tooling.

After installation, add the component from `Add Component > Scene Notes > Scene Note`, or search for `Scene Note`.

Project-owned settings are created under `Assets/SceneNotes/Settings` so package contents can remain read-only.
