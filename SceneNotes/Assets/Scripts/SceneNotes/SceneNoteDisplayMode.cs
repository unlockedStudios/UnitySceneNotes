namespace SceneNotes
{
    /// <summary>
    /// Defines where an editor scene note is shown.
    /// </summary>
    public enum SceneNoteDisplayMode
    {
        SelectedOnly = 0,
        AlwaysVisible = 1,
        HierarchyOnly = 2
    }
}
